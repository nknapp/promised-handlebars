{
  "name": "promised-handlebars",
  "version": "0.3.4",
  "description": "Wrapper for Handlebars that allows helpers returning promises",
  "repository": {
    "type": "git",
    "url": "https://github.com/nknapp/promised-handlebars.git"
  },
  "author": {
    "name": "Nils Knappmeier",
    "email": "npm@knappi.org",
    "url": "https://github.com/nknapp"
  },
  "bugs": {
    "url": "https://github.com/nknapp/promised-handlebars/issues"
  },
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "pretest": "npm list --depth=0 -g standard || npm -g install standard",
    "test": "mocha && standard",
    "preformat": "npm list --depth=0 -g standard || npm -g install standard",
    "format": "standard --format",
    "thought": "thought run -a",
    "prethoughtcheck": "npm list --depth=0 -g thought || npm -g install thought",
    "thoughtcheck": "thought check-engines",
    "version": "npm run thought",
    "preversion": "npm run thoughtcheck"
  },
  "dependencies": {
    "q": "^1.4.1",
    "q-deep": "^1.0.1"
  },
  "standard": {
    "ignore": [
      "spec/fixtures/**"
    ]
  },
  "keywords": [],
  "devDependencies": {
    "chai": "^3.2.0",
    "chai-as-promised": "^5.1.0",
    "get-promise": "^1.4.0",
    "handlebars": "^3.0.3",
    "mocha": "^2.2.5",
    "q-io": "^1.13.1"
  },
  "peerDependencies": {
    "handlebars": "^3.0.3"
  },
  "paths": {
    "relative": "package.json",
    "absolute": "/home/nknappmeier/privat/node-libraries/promised-handlebars/package.json"
  }
}
